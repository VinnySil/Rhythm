<div class="text-white">
    <div id="headers">
        @foreach ($headers as $header)
            <div>{{$header}}</div>
        @endforeach
    </div>
    <div id="items">
        @foreach ($headers as $header)
        <div>{{$header}}</div>
        @endforeach
    </div>
</div>